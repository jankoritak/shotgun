# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Turborepo
.turbo/

# Bundled plugin artifact: generated by `pnpm build`, but COMMITTED so the
# plugin is self-contained at marketplace-install time (PLAN §5). Rebuild and
# re-commit on release if the server source changed.
# (intentionally not ignored: plugin/bin/mcp-server.js)

# Env / secrets (BYOK — never commit X credentials)
.env*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Local tooling artifacts (browser session dumps, scratch)
.playwright-mcp/
/test
